# TanStack Start App

Initialized TanStack Start project with file-based routing.

## Run

```bash
pnpm install
pnpm dev
```

## Build

```bash
pnpm build
```

## Routes

Add route files in `src/routes`.
